---
    interface Props{
        style?: string,
        id?: string,

    }
    const {style, id} = Astro.props
---

<div class={`container mx-auto ${style} px-2 sm:px-0`} id={id}>
    <slot/>
</div>